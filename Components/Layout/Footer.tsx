import React from 'react';
import styles from '../../styles/Global/Layout.module.scss'

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>Hey, this was made with love. Don't steal it. @anewlens</p>
        </footer>
    );
};

export default Footer;